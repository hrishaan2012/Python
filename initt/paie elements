class pair:
    def sum(self,num,target):
        lookup={}

        for i , num in enumerate(num):
            if target-num in lookup:
                return (lookup[target-num],i)
            lookup[num]=i
value=int(input("Enter the target value: "))
print(pair.sum(pair(),[5,10,20,30,40,50],value))